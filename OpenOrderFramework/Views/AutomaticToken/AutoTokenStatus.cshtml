@model IEnumerable<OpenOrderFramework.Models.Token>

@{
    ViewBag.Title = "AutoTokenStatus";
}

<h2>AutoTokenStatus</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TokenID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TokenDT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrderId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TokenID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TokenDT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TokenID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TokenID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TokenID })
        </td>
    </tr>
}

</table>
